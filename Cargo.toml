[package]
name = "cucumber_jetbrains"
version = "0.1.0"
edition = "2021"

[dev-dependencies]
cucumber = "0.19"
futures = "0.3"

[[test]]
name = "example" # this should be the same as the filename of your test target
harness = false  # allows Cucumber to print output instead of libtest